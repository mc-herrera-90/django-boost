=== ":octicons-check-circle-16: Verificar con django-admin"

    ```bash
    (.venv) django-admin --version
    ```

=== ":octicons-terminal-16: Verificar con Python"

    ```bash
    (.venv) python -m django --version
    ```
