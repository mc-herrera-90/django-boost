=== "Crear en una nueva carpeta"

    ```bash
    (.venv) django-admin startproject myproject
    ```

=== "Crear en la carpeta actual"

    ```bash
    (.venv) django-admin startproject myproject .
    ```
