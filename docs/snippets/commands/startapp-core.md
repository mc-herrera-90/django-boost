???+ info "Crear una aplicación"

    Si la aplicación aún no ha sido creada, podemos generar la aplicación `core` con el siguiente comando:

    ```bash title="Terminal"
    python manage.py startapp core
    ```

    
